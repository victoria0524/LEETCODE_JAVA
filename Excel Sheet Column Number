class Solution {
    public int titleToNumber(String s) 
    {
        int res=0,temp=1,len=s.length();

        for(int i=len-1;i>=0;i--)
        {
            res+=(s.charAt(i)-64)*temp;
            temp*=26;
        }
        
        return res;
    }
}
